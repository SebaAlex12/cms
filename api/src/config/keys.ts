// import { Iconfig } from "./interfaces";
import config from "./keys_dev";

// let config: Iconfig;
// process.env.NODE_ENV === "production" ? 
// config = require("./keys_prod") : 
// config = require("./keys_dev");
// console.log('config keys',config);
export default config;